name: VanishExtensions
version: '${version}'
main: io.teamplayer.vanishextensions.VanishExtensions
api-version: '1.20'
dependencies:
  server:
    AdvancedVanish:
      required: true
      load: BEFORE
    PlaceholderAPI:
      required: false
      load: BEFORE